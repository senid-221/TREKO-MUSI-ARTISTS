"use client";
import {useState} from "react"; import Link from "next/link"; import {useRouter} from "next/navigation";
export default function Payment(){const [sent,setSent]=useState(false),[error,setError]=useState("");const router=useRouter();
async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError("");const f=new FormData(e.currentTarget);const r=await fetch("/api/payments",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});const d=await r.json();if(!r.ok){setError(d.error||"Payment submission failed");return}setSent(true)}
return <main className="shell"><nav className="nav"><Link className="brand" href="/">TREKO <span>MUSI</span></Link></nav><section className="section" style={{maxWidth:700}}><div className="card"><div className="eyebrow">Membership Payment</div><h2>Activate your artist account</h2><p className="muted">Pay through MTN Mobile Money, then submit the transaction reference for Treko Musi verification.</p>{error&&<p style={{color:"#ffd400"}}>{error}</p>}{sent?<><h3>Payment submitted ✓</h3><p className="muted">Your payment is pending verification. Once approved, your membership will become active for 30 days.</p><Link href="/dashboard" className="btn primary">Go to Dashboard</Link></>:<form onSubmit={submit} style={{display:"grid",gap:14}}><select name="plan" className="field"><option>Artist</option><option>Growth</option><option>Pro</option></select><input name="phone" className="field" placeholder="MTN Mobile Money number" required/><input name="reference" className="field" placeholder="Payment transaction reference" required/><button className="btn primary">Submit Payment</button></form>}</div></section></main>}