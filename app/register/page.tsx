"use client";
import {useState} from "react"; import {useRouter} from "next/navigation"; import Link from "next/link";
export default function Register(){const router=useRouter();const [error,setError]=useState("");const [loading,setLoading]=useState(false);
async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError("");setLoading(true);const f=new FormData(e.currentTarget);const r=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});const d=await r.json();setLoading(false);if(!r.ok){setError(d.error||"Registration failed");return}router.push("/payment")}
return <main className="shell"><nav className="nav"><Link className="brand" href="/">TREKO <span>MUSI</span></Link></nav><section className="section" style={{maxWidth:650}}><div className="card"><div className="eyebrow">Artist Registration</div><h2>Create your artist account</h2><p className="muted">Create your account, then activate membership with MTN Mobile Money.</p>{error&&<p style={{color:"#ffd400"}}>{error}</p>}<form onSubmit={submit} style={{display:"grid",gap:14}}><input name="stageName" required placeholder="Artist / Stage name" className="field"/><input name="fullName" required placeholder="Full name" className="field"/><input name="email" required type="email" placeholder="Email" className="field"/><input name="password" required minLength={8} type="password" placeholder="Password (8+ characters)" className="field"/><input name="phone" placeholder="WhatsApp number" className="field"/><select name="membershipPlan" className="field"><option>Artist</option><option>Growth</option><option>Pro</option></select><input name="genre" placeholder="Music genre" className="field"/><button disabled={loading} className="btn primary">{loading?"Creating...":"Create Account & Continue"}</button></form><p className="muted">Already registered? <Link href="/login">Login</Link></p></div></section></main>}