"use client";
import {useState} from "react"; import Link from "next/link";
export default function Promotion(){const [sent,setSent]=useState(false),[error,setError]=useState("");
async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError("");const f=new FormData(e.currentTarget);const r=await fetch("/api/promotions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});const d=await r.json();if(!r.ok){setError(d.error||"Request failed");return}setSent(true)}
return <main className="shell"><nav className="nav"><Link className="brand" href="/">TREKO <span>MUSI</span></Link><Link href="/dashboard" className="btn ghost">Dashboard</Link></nav><section className="section" style={{maxWidth:760}}><div className="card"><div className="eyebrow">Audience Growth</div><h2>Request a Promotion Campaign</h2><p className="muted">Available to active members. The Treko Musi team will review your request and confirm the campaign.</p>{error&&<p style={{color:"#ffd400"}}>{error}</p>}{sent?<><h3>Request submitted ✓</h3><p className="muted">Your request is now pending review.</p></>:<form onSubmit={submit} style={{display:"grid",gap:14}}><input name="title" className="field" required placeholder="Campaign / release name"/><textarea name="goal" className="field" required placeholder="What do you want to promote?" rows={5}/><input name="targetAudience" className="field" placeholder="Target audience (e.g. Rwanda, 18–30)"/><input name="platforms" className="field" placeholder="Platforms (Facebook, Instagram, TikTok, YouTube)"/><input name="budget" className="field" type="number" placeholder="Campaign budget (RWF)"/><button className="btn primary">Submit Promotion Request</button></form>}</div></section></main>}