"use client"; import {useState} from "react"; import Link from "next/link";
export default function Promotion(){const [sent,setSent]=useState(false);return <main className="shell"><nav className="nav"><Link className="brand" href="/">TREKO <span>MUSI</span></Link><Link href="/dashboard" className="btn ghost">Dashboard</Link></nav><section className="section" style={{maxWidth:760}}><div className="card"><div className="eyebrow">Audience Growth</div><h2>Request a Promotion Campaign</h2><p className="muted">Tell the Treko Musi team what you want to promote. The team will review your request and contact you about the campaign.</p>{sent?<><h3>Request submitted ✓</h3><p className="muted">Your promotion request is now pending review.</p></>:<form onSubmit={e=>{e.preventDefault();setSent(true)}} style={{display:"grid",gap:14}}><input className="field" required placeholder="Campaign / release name"/><textarea className="field" required placeholder="What do you want to promote?" rows={5}/><input className="field" placeholder="Target audience (e.g. Rwanda, 18–30)"/><input className="field" placeholder="Platforms (Facebook, Instagram, TikTok, YouTube)"/><input className="field" type="number" placeholder="Campaign budget (RWF)"/><button className="btn primary">Submit Promotion Request</button></form>}</div></section></main>}