import Link from "next/link"; import {redirect} from "next/navigation"; import {getCurrentArtist} from "@/lib/auth";
export default async function Dashboard(){const artist=await getCurrentArtist();if(!artist)redirect("/login");return <main className="shell"><nav className="nav"><Link className="brand" href="/">TREKO <span>MUSIC</span></Link><form action="/api/auth/logout" method="post"><button className="btn ghost">Logout</button></form></nav><section className="section artist-profile"><div className="eyebrow">Artist Profile</div><h2>{artist.stageName}</h2><p className="muted">{artist.fullName} · {artist.genre||"Artist"} · {artist.email}</p><div className="profile-grid"><div className="card"><p className="muted">Membership</p><h3>{artist.membershipPlan}</h3><p className="muted">{artist.membershipStatus}</p></div><div className="card"><p className="muted">Community</p><h3>{artist.membershipStatus==="ACTIVE"?"Active":"Pending"}</h3><Link className="btn primary" href="/community">Open Community</Link></div><div className="card"><p className="muted">Promotion</p><h3>Grow audience</h3><Link className="btn primary" href="/plans?service=promotion">Promote Song</Link></div><div className="card"><p className="muted">Distribution</p><h3>Release music</h3><Link className="btn primary" href="/plans?service=distribution">Distribute Song</Link></div></div></section></main>}