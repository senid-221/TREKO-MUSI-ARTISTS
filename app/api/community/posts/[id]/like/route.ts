import {NextResponse} from "next/server"; import {requireArtist} from "@/lib/auth"; import {prisma} from "@/lib/prisma";
export async function POST(_req:Request,{params}:{params:Promise<{id:string}>}){try{const artist=await requireArtist();if(artist.membershipStatus!=="ACTIVE")return NextResponse.json({error:"Active membership is required."},{status:403});const {id}=await params;const existing=await prisma.communityLike.findUnique({where:{postId_artistId:{postId:id,artistId:artist.id}}});if(existing){await prisma.communityLike.delete({where:{id:existing.id}});return NextResponse.json({liked:false})}await prisma.communityLike.create({data:{postId:id,artistId:artist.id}});return NextResponse.json({liked:true})}catch(e:any){return NextResponse.json({error:"Unable to like post."},{status:500})}}