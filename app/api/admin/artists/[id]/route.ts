import {NextResponse} from "next/server"; import {requireAdmin} from "@/lib/auth"; import {prisma} from "@/lib/prisma";
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){try{await requireAdmin();const {id}=await params;const {membershipStatus,role}=await req.json();const data:any={};if(["PENDING","ACTIVE","EXPIRED","CANCELLED"].includes(membershipStatus))data.membershipStatus=membershipStatus;if(["ARTIST","ADMIN"].includes(role))data.role=role;const artist=await prisma.artist.update({where:{id},data});return NextResponse.json({ok:true,artist})}catch(e:any){return NextResponse.json({error:"Unable to update artist."},{status:500})}}