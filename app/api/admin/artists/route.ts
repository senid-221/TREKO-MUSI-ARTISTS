import {NextResponse} from "next/server"; import {requireAdmin} from "@/lib/auth"; import {prisma} from "@/lib/prisma";
export async function GET(){try{await requireAdmin();const artists=await prisma.artist.findMany({orderBy:{createdAt:"desc"},select:{id:true,stageName:true,fullName:true,email:true,phone:true,genre:true,role:true,membershipPlan:true,membershipStatus:true,membershipExpiresAt:true,createdAt:true,_count:{select:{payments:true,releases:true,promotions:true,posts:true}}}});return NextResponse.json({artists})}catch(e:any){return NextResponse.json({error:e?.message==="FORBIDDEN"?"Forbidden":"Unauthorized"},{status:e?.message==="FORBIDDEN"?403:401})}}